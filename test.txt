
            # PySpark JDBC requires queries to be wrapped in parens with an alias.
            # CRITICAL: Oracle SQL does not support the "AS" keyword for table aliases.
            if db_type == 'oracle':
                db_source = f"({query}) query_source"
            else:
