def calculate_score(total, mismatches):
    matched = total - len(mismatches)
    return (matched / total) * 100 if total else 0

def classify_severity(field):
    critical = ["status", "amount", "customerId"]
    return "HIGH" if any(c in field for c in critical) else "LOW"
