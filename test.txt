import subprocess

def send_alert_via_shell(subject, body):
    try:
        subprocess.run(
            ["./send_alert.sh", subject, body],
            check=True
        )
        log("📧 Alert triggered via shell script")

    except subprocess.CalledProcessError as e:
        log(f"❌ Shell alert failed: {e}")
