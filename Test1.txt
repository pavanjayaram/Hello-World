"""
Abstract base class for transformers.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from typing import List

import pandas as pd

from config.models import TransformationRule


class BaseTransformer(ABC):
    """Apply transformation rules to a DataFrame."""

    @abstractmethod
    def transform(self, df: pd.DataFrame, rules: List[TransformationRule]) -> pd.DataFrame:
        """Apply a list of transformation rules and return the result."""
        ...
